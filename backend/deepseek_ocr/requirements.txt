# DeepSeek OCR Dependencies
# Install: pip install -r deepseek_ocr/requirements.txt
# Reference: https://github.com/deepseek-ai/DeepSeek-OCR
transformers==4.46.3
tokenizers==0.20.3
PyMuPDF
img2pdf
einops
easydict
addict
Pillow
numpy

modelscope>=1.18.0

# Flash Attention: only install on Linux
# Windows users will automatically fall back to default attention
flash-attn>=2.7.3; platform_system=="Linux"

